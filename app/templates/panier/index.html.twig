{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="/styles/panier.css" type="text/css">
{% endblock %}

{% block content %}
<main>
    <div class="principale">
        <div class="principale-titre">
            <h3>Mon panier</h3>
        </div>
        <div class="principale-main">
            <div class="product-list">
                {% for produit in panier%}
                    <div  class="produit">
                        <div class="produit-image">
                            <img src="{{ produit.getType().getImage() }}" alt="">
                        </div>
                        <div class="produit-nom">
                            <p>Mon produit</p>
                        </div>
                        <div class="produit-informations">
                            {#<p>Quantite : {{ produit.getQuantite() }}</p>#}
                            <p>Couleur : {{ produit.getCouleur() }}</p>
                            <p>Prix : {{ produit.getType().getPrix()}} $</p>
                        </div>
                        <div class="produit-options">
                            <div class="option-quantite">
                                <a href="" class="btn btn-primary disabled">+</a>
                                <p>1</p>
                                <a href="" class="btn btn-outline-primary disabled">-</a>
                            </div>
                            <div class="option-retirer">
                                <a href="" class="btn btn-warning">Retirer</a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="principale-footer">
            <a href="" class="btn btn-danger">Vider le panier</a>
            <a href="" class="btn btn-check">Valider le panier</a>
        </div>
    </div>
</main>
{% endblock %}
